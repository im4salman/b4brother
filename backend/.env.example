# B4 Brothers Backend Environment Configuration - PostgreSQL Version

# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=b4brothers
DB_USER=postgres
DB_PASSWORD=password

# For production, use a secure password and possibly a connection string:
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production-make-it-at-least-64-chars
JWT_EXPIRES_IN=24h

# Admin Configuration
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@b4brothersinfratech.com
ADMIN_PASSWORD=b4brothers2024

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,https://b4brothersinfratech.com

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@b4brothersinfratech.com

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads/

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# WhatsApp Integration
WHATSAPP_NUMBER=919733221114
WHATSAPP_API_KEY=your-whatsapp-api-key

# Analytics Configuration
GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID
GOOGLE_TAG_MANAGER_ID=GTM_ID

# Security Configuration
TRUST_PROXY=false

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_INTERVAL=86400000
BACKUP_RETENTION_DAYS=30

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Cache Configuration (Redis - Optional)
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# Payment Gateway (Optional for future use)
RAZORPAY_KEY_ID=your-razorpay-key
RAZORPAY_KEY_SECRET=your-razorpay-secret

# SMS Configuration (Optional)
SMS_API_KEY=your-sms-api-key
SMS_SENDER_ID=B4BROS

# Production Database SSL (if required)
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false

# Application URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000
